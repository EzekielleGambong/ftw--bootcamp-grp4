name: "chinook_analysis_grp4"
version: "1.0"
config-version: 2

profile: clickhouse_ftw
model-paths: ["models"]
macro-paths: ["macros"]

models:
  chinook_analysis_grp4:
    clean:
      +schema: clean
      +materialized: table
    mart:
      +schema: mart
      +materialized: view